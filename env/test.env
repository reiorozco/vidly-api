# Test Environment Configuration
# This file is for running tests locally

# Database - Using separate test database
DB=mongodb+srv://reiorozco:0TwdKAJLZ15Gh2Aq@cluster0.enwzurr.mongodb.net/vidly?retryWrites=true&w=majority
# JWT Secret - Use development key for tests
JWT_PRIVATE_KEY=test-secret-key-not-for-production
JWT_EXPIRES_IN=1d

# Server Configuration
HOST=127.0.0.1
PORT=3000
NODE_ENV=test

# Logging - Minimal logging for tests
LOG_LEVEL=error
DEBUG=

# Rate Limiting - Disabled for faster tests
RATE_LIMIT_ENABLED=false

# CORS
CORS_ORIGIN=http://localhost:3000
