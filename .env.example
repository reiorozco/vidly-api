# Vidly API - Environment Configuration Example
# Copy this file to .env and fill in your actual values

# =============================================================================
# DATABASE
# =============================================================================

# MongoDB connection string
# Development: mongodb://localhost:27017/vidly
# Production: mongodb+srv://user:password@cluster.mongodb.net/vidly?retryWrites=true&w=majority&tls=true
# NOTE: Add &tls=true to prevent Node.js 20+ DEP0170 deprecation warning
DB=mongodb://localhost:27017/vidly

# =============================================================================
# AUTHENTICATION & SECURITY
# =============================================================================

# JWT Secret Key (REQUIRED - minimum 32 characters)
# Generate with: openssl rand -base64 32
# IMPORTANT: Use a strong, unique key in production!
JWT_PRIVATE_KEY=your-secret-key-minimum-32-characters-long-replace-this

# JWT Token Expiration (default: 1 day)
JWT_EXPIRES_IN=1d

# =============================================================================
# SERVER CONFIGURATION
# =============================================================================

# Server host (default: 127.0.0.1)
HOST=127.0.0.1

# Server port (default: 3000)
PORT=3000

# Node environment (development | production | test)
NODE_ENV=development

# =============================================================================
# LOGGING
# =============================================================================

# Log level (error | warn | info | debug)
LOG_LEVEL=debug

# Debug namespace (use 'Log' to see all debug messages)
DEBUG=Log

# =============================================================================
# RATE LIMITING
# =============================================================================

# Enable/disable rate limiting (true | false)
# Set to false for development convenience
RATE_LIMIT_ENABLED=false

# Maximum requests per window (default: 100)
RATE_LIMIT_MAX=100

# Window duration in minutes (default: 15)
RATE_LIMIT_WINDOW_MS=15

# =============================================================================
# CORS (Cross-Origin Resource Sharing)
# =============================================================================

# Allowed origins (comma-separated list)
# Development: http://localhost:3000,http://localhost:3001
# Production: https://yourdomain.com,https://www.yourdomain.com
CORS_ORIGIN=http://localhost:3000,http://localhost:3001

# =============================================================================
# OPTIONAL: EXTERNAL SERVICES
# =============================================================================

# Email service (e.g., SendGrid API key)
# SENDGRID_API_KEY=your-sendgrid-api-key

# Redis connection (for caching)
# REDIS_URL=redis://localhost:6379

# Monitoring (e.g., Sentry DSN)
# SENTRY_DSN=your-sentry-dsn

# =============================================================================
# NOTES
# =============================================================================

# 1. Never commit .env to version control (it's in .gitignore)
# 2. Keep .env.example updated when adding new variables
# 3. Use strong, unique values for JWT_PRIVATE_KEY in production
# 4. Set NODE_ENV=production in production deployments
# 5. Enable rate limiting in production (RATE_LIMIT_ENABLED=true)
